spring:
  application:
    name: timer-backend-spring
  profiles:
    default: dev

server:
  port: 8080

jwt:
  refresh:
    expiration: 604800000
  expiration: 900000
  secret: secretsecretsecretsecretsecretsecret

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: when_authorized
      roles: ADMIN
